# Server Scripts CLI Configuration
# Copy to ~/.config/ssc/ssc.env and customize
#
# Usage:
#   cp config/ssc.env.example ~/.config/ssc/ssc.env
#   chmod 600 ~/.config/ssc/ssc.env
#   source ~/.config/ssc/ssc.env

# ============================================================================
# Manifest Configuration
# ============================================================================

# Manifest file location (default: ./manifest.yaml in repository root)
# Useful if you want to use a custom manifest location
# SSC_MANIFEST_FILE="/path/to/custom/manifest.yaml"

# ============================================================================
# Output Configuration
# ============================================================================

# Log level (debug, info, warning, error)
# Controls verbosity of ssc output
SSC_LOG_LEVEL="info"

# Enable colored output (auto, always, never)
# - auto: Color if TTY detected (default)
# - always: Force colors
# - never: Disable colors
SSC_COLOR="auto"

# ============================================================================
# Execution Configuration
# ============================================================================

# Default repository root (optional)
# If not set, auto-detected via git or script location
# SSC_REPO_ROOT="/path/to/repository"

# ============================================================================
# systemd Integration (Optional)
# ============================================================================

# Enable systemd status queries (true/false)
# Set to false to disable systemd integration
SSC_SYSTEMD_ENABLED="true"

# ============================================================================
# Advanced Options
# ============================================================================

# Script execution timeout (seconds)
# Applied to 'ssc run' command (0 = no timeout)
# SSC_EXEC_TIMEOUT=300

# Enable strict validation (true/false)
# Validates all YAML fields against schema
# SSC_STRICT_VALIDATION="false"
